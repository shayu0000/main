<UserControl x:Class="ChargeDischargeSystem.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{StaticResource BackgroundColor}">

    <Grid Margin="12">
        <Border Background="{StaticResource SurfaceColor}"
                BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
            <StackPanel Margin="16">
                <TextBlock Text="系统设置" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>
                <TextBlock Text="通用设置" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>

                <StackPanel Orientation="Horizontal" Margin="0,8">
                    <TextBlock Text="主题:" Width="100" VerticalAlignment="Center"/>
                    <ComboBox Style="{StaticResource MaterialComboBox}" Width="150"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,8">
                    <TextBlock Text="语言:" Width="100" VerticalAlignment="Center"/>
                    <ComboBox Style="{StaticResource MaterialComboBox}" Width="150"/>
                </StackPanel>

                <Button Content="保存设置" Command="{Binding SaveSettingsCommand}" Style="{StaticResource PrimaryButton}"
                        HorizontalAlignment="Left" Padding="16,6" Margin="0,16,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>