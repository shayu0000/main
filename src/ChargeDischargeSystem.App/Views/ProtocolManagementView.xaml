<UserControl x:Class="ChargeDischargeSystem.App.Views.ProtocolManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{StaticResource BackgroundColor}">

    <Grid Margin="12">
        <Border Background="{StaticResource SurfaceColor}"
                BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
            <Grid Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="加载协议" Style="{StaticResource PrimaryButton}" Padding="12,5" Margin="0,0,6,0"/>
                    <Button Content="卸载协议" Style="{StaticResource SecondaryButton}" Padding="12,5"/>
                </StackPanel>

                <DataGrid Grid.Row="1" Style="{StaticResource MaterialDataGrid}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="协议名称" Width="150"/>
                        <DataGridTextColumn Header="版本" Width="100"/>
                        <DataGridTextColumn Header="描述" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>